---

---


<section id="banner" class="lg:px-8 lg:py-8 border border-solid border-x-0 border-y-1">
  <section class="stagger-unit loaded" style="--stagger: 0;">
    <div class="marquee">
      <div class="flex flex-nowrap gap-4">
        <div class="flex-none w-1/3 bg-sky-100 px-4 py-8 rounded-2xl">01</div>
        <div class="flex-none w-1/3 bg-sky-100 px-4 py-8 rounded-2xl">02</div>
        <div class="flex-none w-1/3 bg-sky-100 px-4 py-8 rounded-2xl">03</div>
        <div class="flex-none w-1/3 bg-sky-100 px-4 py-8 rounded-2xl">04</div>
      </div>
    </div>

    <!-- <div class="overflow-x-scroll overflow-hidden">
      <div class="cards marquee grid grid-cols-3 gap-4">
        <div class="card col-span-1 h-16 bg-sky-100 p-4 rounded-2xl">
          text
        </div>
        <div class="card col-span-1 h-16 bg-sky-100 p-4 rounded-2xl">
          text
        </div>
        <div class="card col-span-1 h-16 bg-sky-100 p-4 rounded-2xl">
          text
        </div>
        <div class="card col-span-1 h-16 bg-sky-100 p-4 rounded-2xl">
          text
        </div>
      </div>
    </div> -->
  </section>
</section>

<style>
  .marquee {
    overflow: hidden; /* Hide the content outside the div */
    white-space: nowrap; /* Don't break the content to the next line */
    animation: marquee 30s linear infinite; /* Apply the animation */
  }

  @keyframes marquee {
    0% {
      opacity: 0;
      transform: translateX(-100%); /* Start position: off-screen to the right */
    }
    3% {
      opacity: 1;
    }
    100% {
      transform: translateX(100%); /* End position: off-screen to the left */
    }
}
</style>
